# Wallet Service

## Overview
Manages digital wallets, top-ups, and transactions for users and drivers.

## API Endpoints
- GET /api/v1/wallet/{userId}

## Build & Run
```bash
mvn clean package
java -jar target/wallet-service-1.0.0-SNAPSHOT.jar
```

# User Management Service

## Overview
Manages user profiles, preferences, and identity data.
Does not handle authentication (Auth Service does that).

## Build & Run
```bash
mvn clean package
java -jar target/user-management-service-1.0.0-SNAPSHOT.jar
```

# Webhook Worker Service

## Overview
Async worker to process and dispatch webhooks to subscribers.

## Build & Run
```bash
mvn clean package
java -jar target/webhook-worker-service-1.0.0-SNAPSHOT.jar
```
